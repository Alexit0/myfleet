import React from "react";

const Ref = (props) => {
  return (
    <form>
      <textarea
        type="text"
        placeholder="Loading reference / comments ..."
      ></textarea>
    </form>
  );
};

export default Ref;
